/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['why-not-sustainable-storage-9bab206960816-staging'],
  },
}

module.exports = nextConfig
